CREATE TABLE progress (idprogress INTEGER PRIMARY KEY AUTOINCREMENT UNIQUE, idstudent INTEGER REFERENCES student (idstudent) ON DELETE CASCADE ON UPDATE CASCADE NOT NULL, idsubject INTEGER REFERENCES subject (idsubject) ON DELETE CASCADE ON UPDATE CASCADE NOT NULL, mark INTEGER CHECK (0 < mark <= 10) NOT NULL, datemark INTEGER NOT NULL DEFAULT (strftime('%s', 'now')));
