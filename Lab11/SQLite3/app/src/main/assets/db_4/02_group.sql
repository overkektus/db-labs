CREATE TABLE "group" (idgroup INTEGER PRIMARY KEY AUTOINCREMENT, faculty REFERENCES faculty (faculty) ON DELETE CASCADE ON UPDATE CASCADE, course INTEGER CHECK (0 < course < 6) NOT NULL, name UNIQUE NOT NULL, head TEXT UNIQUE);