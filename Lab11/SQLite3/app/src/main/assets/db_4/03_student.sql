CREATE TABLE student (idstudent INTEGER PRIMARY KEY AUTOINCREMENT, idgroup INTEGER REFERENCES "groups" (idgroup) ON DELETE CASCADE ON UPDATE CASCADE NOT NULL, name TEXT NOT NULL, birthdate INTEGER NOT NULL, address TEXT NOT NULL);
